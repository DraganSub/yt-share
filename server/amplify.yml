version: 1
backend:
  phases:
    build:
      commands:
        - # Add any build commands specific to your backend
    deploy:
      commands:
        - # Add any deploy commands specific to your backend
  artifacts:
    # Specify the artifacts generated by the build phase, if applicable
frontend:
  phases:
    preBuild:
      commands:
        - # Add any pre-build commands specific to your frontend
    build:
      commands:
        - # Add any build commands specific to your frontend
  artifacts:
    # Specify the artifacts generated by the build phase, if applicable
  cache:
    paths:
      - node_modules/**/*

# Specify any custom configuration options for your backend deployment, if needed
# backendConfiguration:
#   customSetting: value